
Simple node server frontend to ssh/grep.


Using:

in celgrepconfig.js edit the ssh user name, and the list of servers.

$ node celgrep.js

Then connect to http://localhost:8888

Args: grep arguments
Grep: search term
Path: path to search
